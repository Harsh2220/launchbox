export interface Token {
    decimals: string
    hub_chain: string
    id: string
    image_url: string
    init_supply: string
    json_config: any
    new_owner: string
    symbol: string
    token_name: string
}
